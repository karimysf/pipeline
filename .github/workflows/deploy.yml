name: CI/CD Pipeline

on:
  push:
    branches:
      - master
    paths:
      - 'src/index.html'


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Trigger
        run: echo "Workflow triggered successfully!"